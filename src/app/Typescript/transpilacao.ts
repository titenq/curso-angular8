// Javascript puro / Vanilla Javscript
var texto = 'Texto de Exemplo';

function soma(num1, num2) {
  return num1 + num2;
}

// ES6 / ES2015
// tslint:disable-next-line: semicolon
let preco = 100.99
// tslint:disable-next-line: semicolon
const PI = 3.141593

let numArray: number[] = [1, 2, 3, 4, 5, 6, 7, 8, 9]

let numTriplo = numArray.map(num => num * 3)

console.log(numTriplo)
