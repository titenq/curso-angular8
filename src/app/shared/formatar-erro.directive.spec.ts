import { FormatarErroDirective } from './formatar-erro.directive';

describe('FormatarErroDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatarErroDirective();
    expect(directive).toBeTruthy();
  });
});
