import { ModoNoturnoDirective } from './modo-noturno.directive'

describe('ModoNoturnoDirective', () => {
  it('should create an instance', () => {
    const directive = new ModoNoturnoDirective();
    expect(directive).toBeTruthy();
  });
});
